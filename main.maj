extern: printInt(number: i32): void;

:add(x: i32, y: i32): i32 {
    return x + y;
}

:main(): i32 {
    a: i32 = 10;
    b: i32 = 20;
    c: i32 = 0;
    c = add(a, b);
    printInt(c);

    while (c < 100) {
        c = c + 1;
        printInt(c);
    }

    return c + 10;
}